apiVersion: v1
kind: Service 
metadata:
  name: golden-app-service
  namespace: demo-app
spec:
  selector:
    app: golden-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP